version: '3.7'
services:
  running_on_dart:
    build: ./
    container_name: running_on_dart
    environment:
      ROD_TOKEN: ${ROD_TOKEN}
      ROD_ADMIN_ID: ${ROD_ADMIN_ID}
      ROD_PREFIX: ${ROD_PREFIX}
# Im not using tags module right now so there is no purpose on attaching volume that
#    volumes:
#    - type: bind
#      source: ./db
#      target: /app/bot/db